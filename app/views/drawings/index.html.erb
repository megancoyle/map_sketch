<h1>What Will You Create Today?</h1>
<div class="space-layout">
  <%= link_to 'Make a New Drawing', drawings_get_address_path, :class => 'get-started' %>
  <div class="all-maps">
    <h2>Featured Drawings</h2>
    <div class="icon-row">
      <div class="feature-icon feature">
        <img src="/images/location-pin.png" alt="Featured Drawings">
      </div>
    </div>
    <p>Sort by: <%= link_to "Title", "?sort=title" %> | <%= link_to "Recently Added", "?sort=created_at" %></p>
    <% @drawings.each do |drawing| %>
      <% if drawing.showcase == true %>
      <div class="map-grid">
        <%= link_to (image_tag drawing.image), drawing_path(drawing) %>
        <h3><%= link_to drawing.title, drawing_path(drawing) %></h3>
      </div>
      <% end %>
    <% end %>
  </div>
</div>
