<%= form_for @drawing do |f| %>

<% if @drawing.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@drawing.errors.count, "error") %> prohibited
        this drawing from being saved:
      </h2>
      <ul>
        <% @drawing.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>

  <p>
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </p>

  <p>
    <%= f.label :location %><br>
    <%= f.text_field :location %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
